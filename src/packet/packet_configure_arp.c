/*
** EPITECH PROJECT, 2019
** spoofing
** File description:
** packet_configure_arp
*/

#include <stdlib.h>
#include "packet.h"

bool packet_configure_arp(packet_t *packet)
{
    if (packet == NULL)
        return (false);
    return (true);
}